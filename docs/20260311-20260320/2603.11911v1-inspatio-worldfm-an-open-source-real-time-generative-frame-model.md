---
title: "InSpatio-WorldFM: An Open-Source Real-Time Generative Frame Model"
title_zh: InSpatio-WorldFM：一个开源实时生成式帧模型
authors: "InSpatio Team, Xiaoyu Zhang, Weihong Pan, Zhichao Ye, Jialin Liu, Yipeng Chen, Nan Wang, Xiaojun Xiang, Weijian Xie, Yifu Wang, Haoyu Ji, Siji Pan, Zhewen Le, Jing Guo, Xianbin Liu, Donghui Shen, Ziqiang Zhao, Haomin Liu, Guofeng Zhang"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.11911v1"
tags: ["query:3d-wm"]
score: 10.0
evidence: 用于空间智能和世界模型的实时生成式帧模型
tldr: InSpatio-WorldFM 是一个开源的实时生成式帧模型，旨在解决视频世界模型在高延迟和空间一致性方面的不足。它采用独立生成每一帧的范式，通过 3D 锚点和隐式空间记忆确保多视图一致性。该模型通过三阶段训练流程（预训练、可控化、蒸馏）实现，支持在消费级 GPU 上进行低延迟的实时空间推理和交互式探索。
selection_source: fresh_fetch
motivation: 传统的视频世界模型依赖序列生成，导致处理窗口级数据时存在显著延迟，难以满足实时空间智能的需求。
method: 采用基于帧的生成范式，结合显式 3D 锚点和隐式空间记忆来维持空间一致性，并通过三阶段流水线将图像扩散模型蒸馏为实时生成器。
result: 实验表明该模型在保持强大空间一致性的同时，能够在消费级 GPU 上实现低延迟的交互式实时模拟。
conclusion: InSpatio-WorldFM 为实时世界模拟提供了一种高效的替代方案，成功平衡了生成质量、空间一致性与推理速度。
---

## 摘要
我们提出了 InSpatio-WorldFM，这是一个用于空间智能的开源实时帧模型。与依赖序列帧生成并因窗口级处理而产生显著延迟的视频世界模型不同，InSpatio-WorldFM 采用基于帧的范式，独立生成每一帧，从而实现低延迟的实时空间推理。通过显式 3D 锚点和隐式空间记忆来强制执行多视图空间一致性，该模型在保持跨视角变化的细粒度视觉细节的同时，保留了全局场景几何结构。我们进一步引入了一个渐进式三阶段训练流水线，通过少步蒸馏将预训练的图像扩散模型转换为可控帧模型，并最终转换为实时生成器。实验结果表明，InSpatio-WorldFM 在支持消费级 GPU 上的交互式探索的同时，实现了强大的多视图一致性，为实时世界模拟提供了一种替代传统视频世界模型的高效方案。

## Abstract
We present InSpatio-WorldFM, an open-source real-time frame model for spatial intelligence. Unlike video-based world models that rely on sequential frame generation and incur substantial latency due to window-level processing, InSpatio-WorldFM adopts a frame-based paradigm that generates each frame independently, enabling low-latency real-time spatial inference. By enforcing multi-view spatial consistency through explicit 3D anchors and implicit spatial memory, the model preserves global scene geometry while maintaining fine-grained visual details across viewpoint changes. We further introduce a progressive three-stage training pipeline that transforms a pretrained image diffusion model into a controllable frame model and finally into a real-time generator through few-step distillation. Experimental results show that InSpatio-WorldFM achieves strong multi-view consistency while supporting interactive exploration on consumer-grade GPUs, providing an efficient alternative to traditional video-based world models for real-time world simulation.

---

## 论文详细总结（自动生成）

这是一份关于论文《InSpatio-WorldFM: An Open-Source Real-Time Generative Frame Model》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的生成式世界模型（World Models）大多基于视频生成架构，存在两个致命缺陷：
    1.  **交互延迟高**：视频模型通常在时间窗口内顺序生成帧，双向注意力和窗口级解码导致推理开销巨大，难以支持真正的实时交互。
    2.  **空间误差累积**：视频模型侧重短期时间连续性，缺乏显式几何约束，导致长期生成中出现结构漂移或几何不一致。
*   **研究背景**：随着空间智能（Spatial Intelligence）需求的增长，业界急需一种既能保持 3D 空间一致性，又能实现低延迟实时响应的生成模型。本文提出的 InSpatio-WorldFM 旨在通过“基于帧（Frame-based）”的生成范式解决上述问题。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：放弃序列帧生成，采用独立生成每一帧的范式。通过**显式 3D 锚点**（点云）和**隐式空间记忆**（参考帧）的混合机制，在单帧生成时引入全局几何约束。
*   **关键技术细节**：
    *   **混合空间记忆（Hybrid Spatial Memory）**：
        *   **显式锚点**：利用点云渲染图作为目标视角的几何底图，提供全局 3D 先验。
        *   **隐式记忆**：通过自注意力机制让模型关注参考帧（Reference Image），提取细粒度的外观细节。
    *   **相机位姿编码（PRoPE）**：采用投影相对位置编码（Projected Relative Positional Encoding），通过相机投影矩阵直接调制注意力机制，增强跨视角的几何推理能力。
    *   **三阶段训练流水线**：
        1.  **Stage I（预训练）**：选用高效的 PixArt-Σ (DiT 架构) 作为基础图像生成器。
        2.  **Stage II（中期训练）**：在真实视频（DL3DV 等）和合成数据（UE 引擎）上训练，引入相机控制和空间记忆。
        3.  **Stage III（后期训练）**：使用分布匹配蒸馏（DMD）技术，将多步去噪模型压缩为 2 步去噪的实时生成器。

### 3. 实验设计
*   **数据集/场景**：
    *   **真实数据**：DL3DV-10K、RealEstate10K 以及互联网视频和自采视频。
    *   **合成数据**：使用 Unreal Engine (UE) 生成具有精确 Ground-truth 位姿和深度的场景，用于微调以提升视角切换的稳定性。
*   **Benchmark 与对比**：
    *   主要进行了**内部消融实验**，对比了三种相机位姿编码策略（Plücker 射线、PRoPE、纯参数化注入）。
    *   对比了蒸馏后的 1 步去噪与 2 步去噪的效果。
    *   定性对比了教师模型（多步）与学生模型（蒸馏后）的一致性。

### 4. 资源与算力
*   **推理算力**：
    *   在 **NVIDIA A100** 上，512×512 分辨率下可达 **10 FPS**，交互延迟约 50-70ms。
    *   在消费级显卡 **RTX 4090** 上，单步推理可达 **7 FPS**。
*   **训练算力**：论文**未明确说明**具体的训练总时长、使用的 GPU 总数或总计算量（FLOPs）。仅提到使用了高效的 DiT 架构以降低计算成本。

### 5. 实验数量与充分性
*   **实验规模**：论文展示了涵盖写实、科幻、游戏、艺术等多种风格的生成结果（图 4-8），并进行了长路径渲染测试以验证几何稳定性。
*   **消融实验**：针对相机编码、噪声调度偏置、渐进式条件注入等关键模块做了对比。
*   **充分性评价**：实验在定性展示和实时性指标上较为充分，证明了其作为“实时帧模型”的可行性。但缺乏与同类闭源模型（如 World Labs 的 RTFM）或其它开源视频世界模型在定量指标（如 PSNR, SSIM, LPIPS 或 FVD）上的直接对比。

### 6. 主要结论与发现
*   **帧模型可行性**：证明了基于帧的独立生成范式配合 3D 锚点，可以达到甚至超过视频模型在空间一致性上的表现，同时消除交互延迟。
*   **蒸馏策略**：发现 **2 步去噪**在保持细节清晰度方面显著优于 1 步去噪，且中间时间步的选择（如 $t_{mid}=200$）对效果至关重要。
*   **合成数据价值**：即使是少量的合成数据微调，也能显著修正真实数据中位姿估计误差带来的不稳定性。

### 7. 优点（亮点）
*   **开源贡献**：作为开源项目，填补了实时生成式帧模型领域的空白。
*   **低延迟交互**：真正实现了消费级 GPU 上的实时世界探索。
*   **架构优雅**：通过 PRoPE 和混合记忆机制，巧妙地在 2D 扩散模型中嵌入了 3D 几何约束。

### 8. 不足与局限
*   **动态内容乏力**：模型目前主要针对静态场景，生成动态物体（如行人、车辆）的能力较弱。
*   **运动边界限制**：依赖于离线生成的全景图或多视角观测，导致在线交互时的移动范围受限。
*   **视觉抖动**：由于帧间缺乏显式的时间平滑约束，在交互过程中仍存在轻微的画面闪烁或抖动（Frame Jitter）。
*   **定量评估缺失**：论文中缺乏大规模的定量 Benchmark 跑分，更多依赖视觉定性评估。

（完）
