---
title: "SK-Adapter: Skeleton-Based Structural Control for Native 3D Generation"
title_zh: SK-Adapter：基于骨架的原生 3D 生成结构控制
authors: "Anbang Wang, Yuzhuo Ao, Shangzhe Wu, Chi-Keung Tang"
date: 2026-03-14
pdf: "https://arxiv.org/pdf/2603.14152v1"
tags: ["query:3d-wm"]
score: 9.0
evidence: 使用骨架对原生3D生成进行结构控制
tldr: 针对原生3D生成模型缺乏精确结构控制的问题，本文提出了SK-Adapter框架。该方法将3D骨架作为核心控制信号，通过轻量级适配器网络将关节坐标和拓扑结构编码为可学习Token，并注入冻结的生成模型中。此外，研究者构建了包含2.4万个样本的Objaverse-TMS数据集。实验证明，SK-Adapter在保持生成质量的同时实现了鲁棒的结构控制，并支持局部3D编辑。
selection_source: fresh_fetch
motivation: 现有的原生3D生成模型难以通过文本或图像提示实现精确的结构化关节控制。
method: 提出一种轻量级适配器，将3D骨架编码为Token并通过交叉注意力机制注入冻结的3D生成骨干网络。
result: 在保持基础模型几何和纹理质量的同时，实现了显著优于基线的结构控制能力，并支持局部区域编辑。
conclusion: SK-Adapter为原生3D生成提供了高效且精确的骨架控制手段，填补了该领域在结构化操纵方面的空白。
---

## 摘要
原生 3D 生成模型在保真度和速度上已取得显著进展，但仍面临一个关键局限：无法指定精确的结构关节，且原生 3D 空间内的精确结构控制尚未得到充分探索。本文提出了 SK-Adapter，这是一个简单且高效的框架，为原生 3D 生成实现了精确的骨架操纵。不同于在描述精确结构时可能存在歧义的文本或图像提示，我们将 3D 骨架视为一类控制信号。SK-Adapter 是一种轻量级的结构适配器网络，它将关节坐标和拓扑结构编码为可学习的 token，并通过交叉注意力机制将其注入冻结的 3D 生成主干网络中。这种巧妙的设计使模型不仅能有效“关注”特定的 3D 结构约束，还能保留其原始的生成先验。为了填补数据空白，我们贡献了 Objaverse-TMS 数据集，这是一个包含 2.4 万个文本-网格-骨架对的大规模数据集。大量实验证明，我们的方法在保持基础模型几何和纹理质量的同时，实现了鲁棒的结构控制，性能显著优于现有基准。此外，我们将该能力扩展到局部 3D 编辑，实现了在骨架引导下对现有资产进行特定区域的编辑，这是以往方法无法实现的。项目主页：https://sk-adapter.github.io/

## Abstract
Native 3D generative models have achieved remarkable fidelity and speed, yet they suffer from a critical limitation: inability to prescribe precise structural articulations, where precise structural control within the native 3D space remains underexplored. This paper proposes SK-Adapter, a simple and yet highly efficient and effective framework that unlocks precise skeletal manipulation for native 3D generation. Moving beyond text or image prompts, which can be ambiguous for precise structure, we treat the 3D skeleton as a first-class control signal. SK-Adapter is a lightweight structural adapter network that encodes joint coordinates and topology into learnable tokens, which are injected into the frozen 3D generation backbone via cross-attention. This smart design allows the model to not only effectively "attend" to specific 3D structural constraints but also preserve its original generative priors. To bridge the data gap, we contribute Objaverse-TMS dataset, a large-scale dataset of 24k text-mesh-skeleton pairs. Extensive experiments confirm that our method achieves robust structural control while preserving the geometry and texture quality of the foundation model, significantly outperforming existing baselines. Furthermore, we extend this capability to local 3D editing, enabling the region specific editing of existing assets with skeletal guidance, which is unattainable by previous methods. Project Page: https://sk-adapter.github.io/

---

## 论文详细总结（自动生成）

这是一份关于论文 **《SK-Adapter: Skeleton-Based Structural Control for Native 3D Generation》** 的深度结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：尽管原生 3D 生成模型（如 Trellis）在生成速度和保真度上取得了突破，但它们在**精确结构控制**方面存在严重缺陷。现有的文本或图像提示难以描述复杂的关节运动（如“膝盖弯曲60度”）或非典型的解剖结构。
*   **核心问题**：如何在不破坏预训练 3D 生成模型强大先验的前提下，将 3D 骨架作为“一类控制信号”直接注入原生 3D 空间，以实现高精度的结构化关节控制。
*   **背景**：传统的 2D 提升方法（如 SKDream）通过 2D 投影骨架引导生成，但这会导致空间歧义、深度丢失和多视图不一致等问题。

### 2. 论文提出的方法论
*   **核心思想**：提出 **SK-Adapter**，一种轻量级的适配器网络。它将 3D 骨架视为稀疏的空间 Token，通过交叉注意力机制（Cross-Attention）将其注入到冻结的 3D 生成主干网络（Trellis）中。
*   **关键技术细节**：
    *   **拓扑感知编码（Topology-Aware Encoding）**：使用图相对位置编码（GRPE）来捕捉骨架的关节坐标和层级连接关系。它通过学习“拓扑距离”和“边缘关系”两种偏置，将骨架转化为富含结构信息的特征向量。
    *   **骨架交叉注意力机制**：在 Flow Transformer 的每个块中引入新的交叉注意力层。主干网络的体素特征作为 Query，骨架特征作为 Key 和 Value，使模型能动态“关注”特定的 3D 结构约束。
    *   **零初始化线性层（Zero-initialized Linear Layer）**：适配器的输出通过零初始化的层注入主干，确保训练初期模型输出为“零信号”，从而完美保留原始模型的生成先验。
    *   **潜在流匹配（Latent Flow Matching）**：在预训练 3D 变分自编码器（VAE）的紧凑潜在空间中进行训练。
*   **算法流程**：输入文本和 3D 骨架 -> 骨架编码器提取特征 -> 注入冻结的 Transformer 进行去噪生成 -> 输出符合骨架约束的 3D 资产。

### 3. 实验设计
*   **数据集**：
    *   **Objaverse-TMS**：作者构建的新数据集，包含 2.4 万个“文本-网格-骨架”三元组，解决了结构引导 3D 生成领域的数据匮乏问题。
    *   **TMS-eval**：包含 140 个测试实例的基准测试集，涵盖类人、动物和物体三大类。
*   **对比方法（Baselines）**：
    *   **SKDream**：基于 2D 投影提升的多视图扩散方法。
    *   **SpaceControl**：一种针对 Trellis 的免训练空间引导方法（作者将其适配为骨架引导）。
*   **评估指标**：
    *   **结构对齐**：ReRigging Score（通过重绑定模型提取骨架并计算与输入骨架的倒角距离）。
    *   **文本一致性**：CLIP Score。
    *   **视觉质量**：PickScore（人类偏好）、KD-DINO（分布保真度）。

### 4. 资源与算力
*   **训练配置**：在 Objaverse-TMS 数据集上训练了 200 个 epoch，Batch Size 为 16，学习率为 $1 \times 10^{-5}$。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号和数量。但提到其推理速度极快，生成单个样本仅需不到 15 秒（与 SpaceControl 相当，远快于 SKDream 的 40 秒）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了大规模的定量对比（140 个测试案例，多维度指标）。
    *   提供了丰富的定性对比图，展示了在复杂姿态下的生成效果。
    *   **消融实验**：针对“交叉注意力机制”和“拓扑感知编码”进行了对比实验，验证了各组件对结构控制和生成质量的贡献。
*   **充分性评价**：实验设计较为全面，不仅对比了不同技术路线（2D 提升 vs 原生 3D），还展示了在 **3D 编辑**（局部添加、重摆姿态）和 **3D 动画**（结合 LBS 蒙皮）中的应用。实验分类（类人/动物/物体）确保了评估的客观性和公平性。

### 6. 论文的主要结论与发现
*   **结构控制力**：SK-Adapter 在 ReRigging Score 上显著优于基线，证明了在原生 3D 空间直接注入骨架信号能有效消除空间歧义。
*   **质量保持**：通过 PEFT（参数高效微调）策略，模型在获得控制力的同时，完全保留了基础模型的高频细节和纹理质量。
*   **高效性**：相比多阶段的 2D 提升方法，SK-Adapter 保持了原生 3D 模型的高推理效率。
*   **编辑潜力**：证明了骨架可以作为一种强有力的局部编辑工具，实现精确的区域修改。

### 7. 优点（亮点）
*   **首创性**：这是首个在原生 3D 生成领域实现精确骨架控制的框架。
*   **轻量化设计**：仅需训练约 1.5 亿参数（相对于庞大的主干网络），且采用非侵入式注入，易于迁移。
*   **数据贡献**：Objaverse-TMS 数据集的发布对社区具有重要价值，尤其是其高质量的专家标注骨架。
*   **应用广泛**：生成的资产直接支持标准的动画管线（Rigging/Skinning）。

### 8. 不足与局限
*   **基础模型依赖**：生成质量（如面部细节、精细纹理）仍受限于预训练主干网络（Trellis）的能力上限。
*   **复杂拓扑挑战**：当骨架极其复杂或关节高度重叠时（如手指交叉），模型可能会产生局部几何混乱。
*   **语义对齐限制**：文本编码器和 3D 基础模型的固有局限可能导致某些复杂的文本属性无法完美呈现。
*   **数据偏差**：虽然有 2.4 万数据，但相比 2D 领域的规模仍较小，可能影响对极罕见姿态的泛化能力。

（完）
