---
title: "Generation Models Know Space: Unleashing Implicit 3D Priors for Scene Understanding"
title_zh: 生成模型懂空间：释放用于场景理解的隐式3D先验
authors: "Xianjin Wu, Dingkang Liang, Tianrui Feng, Kui Xia, Yumeng Zhang, Xiaofan Li, Xiao Tan, Xiang Bai"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.19235v1"
tags: ["query:3d-wm"]
score: 10.0
evidence: 视频生成模型中用于物理动力学的隐式3D先验
tldr: 针对多模态大模型在空间理解和几何推理方面的不足，本文提出 VEGA-3D 框架。该方法不依赖稀缺的显式 3D 数据，而是利用预训练视频生成模型中蕴含的隐式 3D 结构先验。通过从视频扩散模型的中间噪声层提取时空特征，并利用自适应门控融合机制将其与语义表示结合，显著提升了模型在 3D 场景理解、空间推理和具身操作任务中的表现，证明了生成式先验在物理世界理解中的巨大潜力。
selection_source: fresh_fetch
motivation: 多模态大模型在精细几何推理和物理动力学方面存在“空间盲区”，且现有的显式 3D 数据方案面临数据稀缺和泛化挑战。
method: 提出 VEGA-3D 框架，将预训练视频扩散模型作为潜在世界模拟器，通过自适应门控融合机制提取并整合其内部的隐式时空特征。
result: 实验表明，该方法在 3D 场景理解、空间推理和具身操作基准测试中均优于现有最先进模型。
conclusion: 视频生成模型内部蕴含的隐式 3D 先验为物理世界理解提供了可扩展的基础，无需显式 3D 监督即可增强模型的空间感知。
---

## 摘要
尽管多模态大语言模型（MLLMs）展现出令人印象深刻的语义能力，但它们往往面临“空间盲区”问题，在细粒度几何推理和物理动力学方面表现欠佳。现有的解决方案通常依赖于显式的3D模态或复杂的几何支架，这受到数据稀缺和泛化挑战的限制。在这项工作中，我们提出了一种范式转变，即利用大规模视频生成模型中的隐式空间先验。我们假设，为了合成时间上连贯的视频，这些模型本质上学习了鲁棒的3D结构先验和物理规律。我们介绍了 VEGA-3D（视频提取生成感知），这是一个即插即用的框架，它将预训练的视频扩散模型重新利用为潜世界模拟器（Latent World Simulator）。通过从中间噪声水平提取时空特征，并通过令牌级自适应门控融合机制将其与语义表示集成，我们在没有显式3D监督的情况下，为 MLLMs 丰富了密集的几何线索。在3D场景理解、空间推理和具身操纵基准测试上的广泛实验表明，我们的方法优于最先进的基准模型，验证了生成先验为物理世界理解提供了可扩展的基础。代码已在 https://github.com/H-EmbodVis/VEGA-3D 公开。

## Abstract
While Multimodal Large Language Models demonstrate impressive semantic capabilities, they often suffer from spatial blindness, struggling with fine-grained geometric reasoning and physical dynamics. Existing solutions typically rely on explicit 3D modalities or complex geometric scaffolding, which are limited by data scarcity and generalization challenges. In this work, we propose a paradigm shift by leveraging the implicit spatial prior within large-scale video generation models. We posit that to synthesize temporally coherent videos, these models inherently learn robust 3D structural priors and physical laws. We introduce VEGA-3D (Video Extracted Generative Awareness), a plug-and-play framework that repurposes a pre-trained video diffusion model as a Latent World Simulator. By extracting spatiotemporal features from intermediate noise levels and integrating them with semantic representations via a token-level adaptive gated fusion mechanism, we enrich MLLMs with dense geometric cues without explicit 3D supervision. Extensive experiments across 3D scene understanding, spatial reasoning, and embodied manipulation benchmarks demonstrate that our method outperforms state-of-the-art baselines, validating that generative priors provide a scalable foundation for physical-world understanding. Code is publicly available at https://github.com/H-EmbodVis/VEGA-3D.

---

## 论文详细总结（自动生成）

### 论文总结：Generation Models Know Space (VEGA-3D)

#### 1. 核心问题与整体含义
论文针对多模态大语言模型（MLLM）在处理3D空间任务时存在的**“空间盲区”（Spatial Blindness）**问题展开研究。尽管现有的 MLLM 语义理解能力极强，但在精细几何推理、物理动力学理解和空间定位方面表现较差。
*   **研究动机**：现有的提升空间感知的方法通常依赖显式的 3D 数据（如点云、深度图）或复杂的几何重建算法，这些方法受限于 3D 标注数据的稀缺性和泛化能力的不足。
*   **核心假设**：大规模视频生成模型（如 Sora, Wan2.1）为了生成时间连贯、物理真实的视频，其内部表征必然隐式地学习了 3D 结构先验和物理规律。因此，可以将视频生成模型作为“潜在世界模拟器”，提取其隐式先验来增强 MLLM。

#### 2. 方法论
论文提出了 **VEGA-3D (Video Extracted Generative Awareness)** 框架，这是一个即插即用的双分支架构：
*   **潜在世界模拟器（Latent World Simulator）**：使用预训练且冻结的视频扩散模型（如 Wan2.1）作为生成分支。
*   **噪声注入与特征提取**：
    *   将输入视频通过 VAE 编码到潜空间。
    *   利用**流匹配（Flow Matching）**路径注入适量噪声（实验发现中间噪声水平 $t \approx 0.3$ 效果最好），迫使模型激活其内部的去噪/结构恢复能力。
    *   从扩散模型的中间层（如 DiT 的第 20 层）提取时空特征，这些特征包含了丰富的几何一致性信息。
*   **令牌级自适应门控融合（Token-level Adaptive Gated Fusion）**：
    *   设计了一个门控机制，通过 Sigmoid 函数计算每个 Token 的权重 $g_i$。
    *   公式：$F_{fused} = (1 - g_i) \cdot F_{gen} + g_i \cdot F_{sem}$。
    *   该机制允许模型根据任务需求，动态地在语义特征（来自 SigLIP 等编码器）和几何特征（来自生成模型）之间进行权衡。

#### 3. 实验设计
研究在三个关键领域进行了广泛验证：
*   **3D 场景理解**：在 **ScanNet** 数据集基础上，评估了 **ScanRefer**（空间定位）、**Multi3DRefer**（多目标定位）、**Scan2Cap**（密集描述）、**ScanQA** 和 **SQA3D**（空间问答）。
*   **空间推理**：使用 **VSI-Bench**，涵盖物体计数、绝对距离、相对方向、路径规划等 8 个子任务。
*   **具身操纵（Embodied AI）**：在 **LIBERO** 机器人操作基准上测试了策略生成的成功率。
*   **对比方法**：包括 Video-3D LLM, 3DRS, Inst3D-LLM, LLaVA-3D 等最先进的（SOTA）通用和专用模型。

#### 4. 资源与算力
*   **硬件环境**：论文明确指出实验使用了 **8 张 NVIDIA H100 GPU**。
*   **训练细节**：采用 Adam 优化器，Batch Size 为 128，学习率在 $1 \times 10^{-5}$（语言模型）到 $2 \times 10^{-6}$（视觉分支）之间。
*   **推理开销**：论文承认引入视频扩散分支会增加推理延迟和显存占用，但通过特征缓存（Caching）技术，可以将每场景的额外延迟显著降低。

#### 5. 实验数量与充分性
实验设计非常充分且具有说服力：
*   **多维度验证**：不仅涵盖了静态 3D 场景理解，还扩展到了动态视频推理和闭环机器人控制。
*   **消融实验**：深入探讨了不同生成模型（UNet vs. DiT）、不同噪声水平、不同网络深度以及不同融合策略对性能的影响。
*   **客观性分析**：引入了“多视图一致性得分”（Multi-view Correspondence Score），定量证明了生成模型的几何一致性与下游 3D 任务表现之间存在强正相关性。

#### 6. 主要结论与发现
*   **生成模型懂空间**：视频生成模型（尤其是基于 DiT 架构的模型）内部确实蕴含了极强的 3D 结构先验，其多视图一致性得分高达 96% 以上。
*   **中间层最有效**：最有效的几何先验并非来自最终生成的像素，而是来自扩散过程的中间噪声阶段和网络的中间层。
*   **互补性**：生成特征与语义特征具有高度互补性，融合后的模型在定位精度（Acc@0.5）和空间问答（EM）上提升显著。
*   **可扩展性**：随着视频生成模型能力的提升，VEGA-3D 的性能也随之增强，展现了良好的 Scaling 潜力。

#### 7. 优点
*   **范式创新**：摆脱了对昂贵 3D 标注数据的依赖，开辟了利用生成模型作为物理世界先验的新路径。
*   **即插即用**：框架结构简单，可以轻松集成到现有的 MLLM（如 Qwen2-VL, LLaVA）中。
*   **性能卓越**：在不使用任何显式 3D 监督的情况下，在多个基准测试中刷新了记录。

#### 8. 不足与局限
*   **计算成本**：尽管有缓存机制，但运行一个巨大的视频扩散模型作为特征提取器，其显存和计算开销仍高于传统的视觉编码器。
*   **语义-几何权衡**：在某些纯语义任务（如 Scan2Cap 的某些词汇指标）中，引入几何先验可能会带来轻微的性能波动。
*   **场景限制**：目前主要在室内场景（ScanNet）和模拟环境（LIBERO）中验证，在极端动态或超大规模室外场景下的表现尚待进一步探索。

（完）
